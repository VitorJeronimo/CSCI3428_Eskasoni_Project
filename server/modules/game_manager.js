// A GameManager instance will be created by a Lobby instance and it will take
// care of generating category lists, a new letter for the current round and 
// the scoring phase at the end of each match
class GameManager {
 
}

// Get round ready